AWSTemplateFormatVersion: "2010-09-09"

Description: Template generated by rain

Resources:
  IAMRoleAWSCloudFormationStackSetAdministrationRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: cloudformation.amazonaws.com
            Action: sts:AssumeRole
      Description: managed by cloudformation
      MaxSessionDuration: 3600
      Path: /
      # PermissionsBoundary: CHANGEME
      Policies:
        - PolicyName: iam-cloudformation
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sts:AssumeRole
                Resource:
                  - arn:aws:iam::*:role/AWSCloudFormationStackSetExecutionRole
      RoleName: AWSCloudFormationStackSetAdministrationRole
      # Tags:
      #   - Key: CHANGEME
      #     Value: CHANGEME

  IAMRoleAWSCloudFormationStackSetExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              AWS: !Ref AWS::AccountId
            Action: sts:AssumeRole
      Description: managed by cloudformation
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AdministratorAccess
      MaxSessionDuration: 3600
      Path: /
      # PermissionsBoundary: CHANGEME
      RoleName: AWSCloudFormationStackSetExecutionRole
      # Tags:
      #   - Key: CHANGEME
      #     Value: CHANGEME
